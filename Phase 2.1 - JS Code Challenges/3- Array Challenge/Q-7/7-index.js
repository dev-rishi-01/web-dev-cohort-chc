function findPhone(items) {
  // Return the index of "Phone" in the items array
  return items.indexOf("Phone");
} //            0        1       2          3
let items = ["wallet", "keys", "Phone", "Glasses"];
console.log(findPhone(items));

